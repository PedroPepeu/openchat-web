const button_send_info = getElementById("button_login");

function check_get_info(email, password) {
    
}

button_send_info.addEventListener("click", function () {
    const email_get_info = getElementById("email_login");
    const password_get_info = getElementById("password_login");

    if(!check_get_info(email_get_info, password_get_info)) {

    }
});

